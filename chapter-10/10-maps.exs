IO.puts("")
IO.puts("# = = = = = = = = = = ")
IO.puts("# Chapter 10")
IO.puts("# Name: Working with maps")
IO.puts("# File: 10-maps.exs")
IO.puts("# = = = = = = = = = = ")
IO.puts("")

IO.puts(first_sentence = "Put first sentence: " |> IO.gets |> String.trim)
IO.puts(second_sentence = "Put second sentence: " |> IO.gets |> String.trim)
IO.puts(third_sentence = "Put third sentence: " |> IO.gets |> String.trim)

IO.puts("")
